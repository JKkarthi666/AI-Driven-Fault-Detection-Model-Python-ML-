# AI-Driven Fault Detection Model


Simple end-to-end example that generates synthetic power/thermal sensor data, trains a RandomForest classifier to detect anomalies, and provides a streaming inference script.


## Quickstart


1. Create a virtual environment and install requirements:


```bash
python -m venv venv
source venv/bin/activate # or venv\Scripts\activate on Windows
pip install -r requirements.txt